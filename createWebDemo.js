// output goes into the gh pages branch

// steps to create a static demo version of the server

// 1: yarn web-build, then copy dist_web/* to gh-pages/
// 2: copy in static/* to gh-pages/
// 3: copy in example music files to gh-pages/music/
// 4: create and copy in cache files for each music file to gh-pages/tags/music/
// 5: copy main.html to index.html

// 6: copy in the github domain thing because this will have to run on like music.pfg.pw so the fetches with / work